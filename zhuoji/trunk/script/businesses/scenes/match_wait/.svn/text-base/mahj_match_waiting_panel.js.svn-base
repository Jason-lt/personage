/**
 *  比赛，赛间等待面板
 * Created by 许敬 on 2017/7/7.
 */

(function () {
    var GLOBAL_OBJ = guiyang;

    GLOBAL_OBJ.businesses.scenes.MatchWait.MatchWaitingPanel = GLOBAL_OBJ.bkernel.base.BaseController.extend({
        _TAG: "businesses.scenes.MatchWait.MatchWaitingPanel",
        ctor: function (type) {
            this._super();
            if(type == 1){
                this.init(GLOBAL_OBJ.RES.MAHJ_MATCH_DENGDAI_CCBI);//配桌中
            }else if(type == 2){
                this.init(GLOBAL_OBJ.RES.MAHJ_MATCH_TABLE_SHOW_TIME_LUNKONG_CCBI);//轮空
            }
        },

        onLoad: function () {
            this._super();
        },

        onCleanup:function () {
            this._super();
        }
    });

})();